<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Login</title>
    <%- include('partials') %>
</head>
<body>
    <div class="container">
        <h1>Login</h1>
        <% if (error) { %>
            <div class="error"><%= error %></div>
        <% } %>
        <form action="/login" method="POST">
            <label>Usuário:
                <input type="text" name="username" required>
            </label>
            <label>Senha:
                <input type="password" name="password" required>
            </label>
            <button type="submit">Entrar</button>
        </form>
        <div style="margin-top:18px;">
            <span>Não tem conta?</span> <a href="/register">Registre-se</a>
        </div>
    </div>
</body>
</html>